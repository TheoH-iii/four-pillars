---
phase: 04-bilingual-ui-production-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "Page background is warm cream (#FDF7EC), not dark"
    - "Cards render on white (#FFFFFF) with gold border accents"
    - "Form inputs, buttons, and labels use the gold/cream palette"
    - "Font switches between Playfair Display/Inter (EN) and Noto Serif TC/Noto Sans TC (zh) via body.lang-zh"
    - "Responsive grid preserved: 3-col on desktop, 2-col on tablet, 1-col on mobile"
    - "Language toggle button styled correctly via .lang-btn and .lang-btn.active"
  artifacts:
    - path: "style.css"
      provides: "Full warm cream theme ported from v1-backup, adapted to current HTML selectors"
      contains: "--bg-page: #FDF7EC"
  key_links:
    - from: "body.lang-zh"
      to: "Noto Serif TC / Noto Sans TC fonts"
      via: "CSS selector body.lang-zh h1, body.lang-zh .card-title"
      pattern: "lang-zh"
    - from: ".bazi-card"
      to: "v1-backup .card styles"
      via: "CSS selector mapping"
      pattern: "bazi-card"
---

<objective>
Replace the current dark theme style.css with a full port of the v1-backup warm cream palette, adapted to the current HTML element selectors (.bazi-card, #form-screen, #chart-screen, etc.).

Purpose: Satisfies UI-04 — chart displays beautifully with responsive design.
Output: style.css with complete warm cream design system.
</objective>

<execution_context>
@/home/theohuang/.claude/get-shit-done/workflows/execute-plan.md
@/home/theohuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current HTML selectors in index.html and app.js that CSS must target -->

<!-- index.html static selectors -->
#form-screen          — form page wrapper
#chart-screen         — chart page wrapper
#birth-form           — the form element
.field-group          — label + input wrapper
.date-fields          — 3-col grid for year/month/day inputs
.time-fields          — 2-col grid for hour/minute inputs
.city-wrapper         — position:relative wrapper for city input + suggestions
#city-suggestions     — autocomplete dropdown ul > li
#submit-btn           — primary CTA button
#back-btn             — back to form button
#form-error           — error message paragraph
#lang-toggle          — fixed language toggle (added in Plan 01)
.lang-btn             — individual EN / 中文 buttons
.lang-btn.active      — active language button

<!-- app.js dynamic selectors (renderCards output) -->
.bazi-card            — each card wrapper div
.card-title           — h2 inside each card
.card-body            — div with data rows
.data-row             — label+value row div
.data-label           — left label span
.data-value           — right value span
.reading-container    — AI reading output area
.reveal-btn           — "Reveal Reading" button
.reading-cursor       — blinking cursor span during streaming
.reading-error        — error state div
.retry-btn            — retry button inside reading-error

<!-- v1-backup CSS variables to port (exact values) -->
--bg-page: #FDF7EC
--bg-card: #FFFFFF
--gold: #C9974A
--gold-light: #F0E0C0
--gold-glow: rgba(201,151,74,0.35)
--gold-border: rgba(201,151,74,0.4)
--wood: #A8C5A0  --wood-bg: rgba(168,197,160,0.15)
--fire: #E8A89C  --fire-bg: rgba(232,168,156,0.15)
--earth: #D4BC8A --earth-bg: rgba(212,188,138,0.15)
--metal: #C8C8B0 --metal-bg: rgba(200,200,176,0.15)
--water: #96B4C8 --water-bg: rgba(150,180,200,0.15)
--text: #2C2213
--text-sub: #6B5B45
--text-dim: #A89880
--border: rgba(201,151,74,0.2)
--shadow: 0 4px 20px rgba(0,0,0,0.06)
--shadow-md: 0 8px 32px rgba(0,0,0,0.10)
--radius: 16px
--radius-sm: 10px
--font-en-serif: 'Playfair Display', Georgia, serif
--font-en-sans: 'Inter', system-ui, sans-serif
--font-zh-serif: 'Noto Serif TC', 'Playfair Display', serif
--font-zh-sans: 'Noto Sans TC', 'Inter', sans-serif
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace style.css with v1-backup warm cream theme adapted to current selectors</name>
  <files>style.css</files>
  <action>
Fully replace style.css. Do NOT keep any of the current dark theme rules. Port v1-backup CSS variables and design system, then write rules for the current HTML selectors.

Structure to implement:

1. CSS Variables (:root) — port all v1-backup variables exactly as listed in interfaces above.

2. Reset — *, box-sizing: border-box, margin/padding 0.

3. Body — background: var(--bg-page); color: var(--text); font-family: var(--font-en-sans); min-height: 100vh.
   body.lang-zh — font-family: var(--font-zh-sans).

4. Typography — h1, h2, h3 use var(--font-en-serif).
   body.lang-zh h1, h2, h3 use var(--font-zh-serif).

5. #form-screen layout:
   - max-width: 520px; margin: 0 auto; padding: 60px 20px 40px.
   - h1 inside: font-family var(--font-en-serif); font-size clamp(28px,5vw,42px); color var(--text); text-align center; margin-bottom 32px; line-height 1.2.
   - body.lang-zh #form-screen h1: font-family var(--font-zh-serif).

6. #birth-form — background var(--bg-card); border-radius var(--radius); box-shadow var(--shadow-md); padding 32px 28px.

7. .field-group — margin-bottom 20px.
   .field-group > label — display block; font-size 11px; font-weight 500; letter-spacing 1px; text-transform uppercase; color var(--text-dim); margin-bottom 8px.

8. .date-fields — display grid; grid-template-columns 1fr 1fr 1fr; gap 10px.
   .time-fields — display grid; grid-template-columns 1fr 1fr; gap 10px.

9. input[type=number], input[type=text], select:
   width 100%; padding 11px 14px; background var(--bg-page); border 1px solid var(--border);
   border-radius var(--radius-sm); color var(--text); font-size 14px; font-family inherit;
   appearance none; outline none; transition border-color 0.2s.
   :focus — border-color var(--gold); box-shadow 0 0 0 3px var(--gold-glow).

10. .city-wrapper — position relative.
    #city-suggestions — position absolute; top calc(100% + 4px); left 0; right 0;
    background var(--bg-card); border 1px solid var(--border); border-radius var(--radius-sm);
    box-shadow var(--shadow-md); z-index 50; max-height 200px; overflow-y auto; list-style none; padding 0; margin 0.
    #city-suggestions li — padding 9px 14px; font-size 13px; cursor pointer; color var(--text).
    #city-suggestions li:hover — background var(--bg-page); color var(--gold).

11. #submit-btn:
    width 100%; padding 14px; margin-top 8px;
    background linear-gradient(135deg, #C9974A, #B8823A); color #fff; border none;
    border-radius var(--radius-sm); font-size 15px; font-weight 600;
    font-family var(--font-en-serif); letter-spacing 1px; cursor pointer; transition all 0.25s.
    body.lang-zh #submit-btn — font-family var(--font-zh-serif).
    :hover — background linear-gradient(135deg, #B8823A, #A06F28); box-shadow 0 6px 24px var(--gold-glow); transform translateY(-1px).
    :disabled — opacity 0.6; cursor not-allowed; transform none.

12. #form-error — color #c0392b; font-size 13px; margin-top 10px; text-align center.

13. #chart-screen — padding 20px 20px 60px.

14. #back-btn:
    display inline-flex; align-items center; gap 6px; margin-bottom 24px;
    background transparent; border 1px solid var(--border); border-radius 20px;
    padding 8px 16px; color var(--text-sub); font-size 13px; cursor pointer; transition all 0.2s.
    :hover — border-color var(--gold); color var(--gold).

15. #cards-container — display grid; grid-template-columns repeat(3, 1fr); gap 16px.
    @media (max-width: 900px) — grid-template-columns repeat(2, 1fr).
    @media (max-width: 560px) — grid-template-columns 1fr.

16. .bazi-card:
    background var(--bg-card); border-radius var(--radius); box-shadow var(--shadow);
    border 1px solid var(--border); padding 24px 20px;
    display flex; flex-direction column; gap 12px.
    :hover — box-shadow var(--shadow-md); border-color var(--gold-border); transition all 0.2s.

17. .card-title:
    font-family var(--font-en-serif); font-size 12px; font-weight 600;
    letter-spacing 1.5px; text-transform uppercase; color var(--gold); margin 0.
    body.lang-zh .card-title — font-family var(--font-zh-sans); letter-spacing 2px.

18. .card-body — flex 1.

19. .data-row — display flex; justify-content space-between; align-items baseline;
    padding 6px 0; border-bottom 1px solid var(--border); font-size 13px.
    .data-row:last-child — border-bottom none.
    .data-label — color var(--text-dim); font-size 12px; flex-shrink 0; margin-right 8px.
    .data-value — color var(--text); text-align right; font-weight 500.

20. .reveal-btn:
    width 100%; padding 10px; background transparent;
    border 1px solid var(--gold-border); border-radius var(--radius-sm);
    color var(--gold); font-size 12px; font-weight 500; cursor pointer; transition all 0.2s.
    body.lang-zh .reveal-btn — font-family var(--font-zh-sans).
    :hover — background rgba(201,151,74,0.08); box-shadow 0 0 8px var(--gold-glow).

21. .reading-container — font-size 13px; line-height 1.7; color var(--text-sub).
    .reading-container p — margin-bottom 10px.
    .reading-container p:last-child — margin-bottom 0.
    .reading-container ul — padding-left 18px; margin-bottom 10px.
    .reading-container li — margin-bottom 4px.
    .reading-container strong — color var(--text); font-weight 600.

22. .reading-cursor — display inline-block; width 2px; height 14px;
    background var(--gold); margin-left 2px; vertical-align middle;
    animation blink 1s step-end infinite.
    @keyframes blink — 0%,100% opacity 1; 50% opacity 0.

23. .reading-error — background rgba(192,57,43,0.06); border 1px solid rgba(192,57,43,0.2);
    border-radius var(--radius-sm); padding 12px 14px; font-size 13px; color #c0392b.
    .retry-btn — margin-top 8px; padding 6px 14px; background transparent;
    border 1px solid #c0392b; border-radius 20px; color #c0392b; font-size 12px; cursor pointer.
    .retry-btn:hover — background rgba(192,57,43,0.08).

24. .lang-btn — border none; background transparent; color var(--text-dim);
    font-size 12px; font-weight 500; padding 4px 10px; border-radius 16px; cursor pointer; transition all 0.2s.
    .lang-btn.active — background var(--gold); color #fff.
    .lang-btn:hover:not(.active) — color var(--gold).

25. .error-msg — color #c0392b; font-size 13px; margin-top 8px.

26. @media (max-width: 560px):
    #form-screen — padding 48px 16px 32px.
    #birth-form — padding 24px 18px.
    .date-fields — grid-template-columns 1fr 1fr; and last child spans full width.
    #cards-container — grid-template-columns 1fr.
  </action>
  <verify>Open index.html in browser. Page background is cream (#FDF7EC). Form renders in white card with gold accents. Cards grid shows 3 columns on wide viewport, collapses on mobile. No dark background visible anywhere.</verify>
  <done>style.css contains warm cream theme; all current HTML selectors styled; responsive grid works; lang-zh font switching rules present</done>
</task>

</tasks>

<verification>
- Page background: #FDF7EC (cream), not dark
- Form card: white background, gold border/focus states
- Cards grid: 3-col → 2-col → 1-col responsive
- body.lang-zh switches to Noto fonts
- .lang-btn.active shows gold background
- No broken layout or missing styles
</verification>

<success_criteria>
Visual inspection confirms warm cream palette throughout. Responsive grid collapses correctly. Font switching works with body.lang-zh class.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bilingual-ui-production-verification/04-02-SUMMARY.md`
</output>
