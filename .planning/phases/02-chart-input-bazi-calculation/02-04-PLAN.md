---
phase: 02-chart-input-bazi-calculation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [app.js]
autonomous: true
requirements: [CHART-06]
gap_closure: true

must_haves:
  truths:
    - "Yang day master partner element is the element that controls the day master's element (财星 logic)"
    - "Yin day master partner element is the element that the day master's element controls (官星 logic)"
    - "The two branches of the isYang ternary in buildCardPartnerTraits() produce different values for any day master whose controlling and controlled elements differ"
  artifacts:
    - path: "app.js"
      provides: "Corrected buildCardPartnerTraits() with distinct yang/yin branches"
      contains: "ELEMENT_CONTROLS[dayEl]"
  key_links:
    - from: "app.js buildCardPartnerTraits()"
      to: "bazi-engine.js ELEMENT_CONTROLS"
      via: "ELEMENT_CONTROLS[dayEl] for yin branch"
      pattern: "ELEMENT_CONTROLS\\[dayEl\\]"
---

<objective>
Fix the dead ternary in buildCardPartnerTraits() so yang and yin day masters produce different partner element logic.

Purpose: The Ideal Partner Traits card currently shows the same partner element regardless of day master polarity because both branches of the isYang ternary execute identical code. This is incorrect Bazi logic.
Output: app.js with corrected yin/yang partner element derivation.
</objective>

<execution_context>
@/home/theohuang/.claude/get-shit-done/workflows/execute-plan.md
@/home/theohuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dead ternary in buildCardPartnerTraits()</name>
  <files>app.js</files>
  <action>
    In app.js at lines 327-329, replace the dead ternary with two distinct expressions:

    - Yang branch (财星 logic): the element that CONTROLS the day master's element.
      Expression: `Object.keys(ELEMENT_CONTROLS).find(k => ELEMENT_CONTROLS[k] === dayEl)`
      (unchanged from current — this branch is already correct)

    - Yin branch (官星 logic): the element that the day master's element CONTROLS.
      Expression: `ELEMENT_CONTROLS[dayEl]`

    Result should be:
    ```js
    const partnerEl = isYang
      ? Object.keys(ELEMENT_CONTROLS).find(k => ELEMENT_CONTROLS[k] === dayEl)
      : ELEMENT_CONTROLS[dayEl];
    ```

    No other changes to the function or file.
  </action>
  <verify>
    <automated>node -e "
const ELEMENT_CONTROLS = { wood:'earth', fire:'metal', earth:'water', metal:'wood', water:'fire' };
const STEM_ELEMENT = { '甲':'wood','乙':'wood','丙':'fire','丁':'fire','戊':'earth','己':'earth','庚':'metal','辛':'metal','壬':'water','癸':'water' };

function partnerEl(stem) {
  const isYang = ['甲','丙','戊','庚','壬'].includes(stem);
  const dayEl = STEM_ELEMENT[stem];
  return isYang
    ? Object.keys(ELEMENT_CONTROLS).find(k => ELEMENT_CONTROLS[k] === dayEl)
    : ELEMENT_CONTROLS[dayEl];
}

// Yang 甲 (wood): controlled by metal
console.assert(partnerEl('甲') === 'metal', 'yang 甲 should be metal');
// Yin 乙 (wood): controls earth
console.assert(partnerEl('乙') === 'earth', 'yin 乙 should be earth');
// Yang 丙 (fire): controlled by water
console.assert(partnerEl('丙') === 'water', 'yang 丙 should be water');
// Yin 丁 (fire): controls metal
console.assert(partnerEl('丁') === 'metal', 'yin 丁 should be metal');
// Yang and yin of same element must differ
console.assert(partnerEl('甲') !== partnerEl('乙'), 'yang/yin must differ');
console.log('All assertions passed');
"
    </automated>
  </verify>
  <done>Yang and yin day masters return different partner elements. The node assertion script exits 0 with "All assertions passed".</done>
</task>

</tasks>

<verification>
Run the inline node assertion from the verify block. All 5 assertions must pass.
Confirm app.js line 329 now reads `ELEMENT_CONTROLS[dayEl]` (not a repeat of the yang expression).
</verification>

<success_criteria>
- buildCardPartnerTraits() yin branch uses ELEMENT_CONTROLS[dayEl]
- Yang branch unchanged (Object.keys find)
- A yang stem (e.g. 甲) and its yin counterpart (乙) produce different partnerEl values
- No other lines in app.js modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-chart-input-bazi-calculation/02-04-SUMMARY.md`
</output>
