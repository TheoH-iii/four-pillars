---
phase: 01-project-scaffold-deployment-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - style.css
  - app.js
  - bazi-engine.js
  - solar.js
  - cities.js
  - i18n.js
  - api/reading.js
  - vercel.json
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "All scaffold files exist at their specified paths"
    - "index.html is valid HTML5 with no framework dependencies"
    - "GET /api/reading returns { status: 'ok', message: 'reading endpoint ready' }"
    - "vercel.json configures 30-second function timeout"
  artifacts:
    - path: "index.html"
      provides: "HTML entry point"
      contains: "<!DOCTYPE html>"
    - path: "api/reading.js"
      provides: "Serverless function stub"
      exports: ["default handler"]
    - path: "vercel.json"
      provides: "Vercel configuration"
      contains: "functions"
  key_links:
    - from: "index.html"
      to: "app.js, style.css, bazi-engine.js, solar.js, cities.js, i18n.js"
      via: "script/link tags"
      pattern: "<script src="
    - from: "api/reading.js"
      to: "vercel.json"
      via: "functions config"
      pattern: "api/reading.js"
---

<objective>
Create the complete file scaffold for the Four Pillars Bazi web app: HTML entry point, CSS, JS stub files, the /api/reading serverless function stub, and Vercel configuration.

Purpose: Establishes the file structure that all future phases build on. No app logic — just the skeleton that deploys cleanly.
Output: 9 files at their locked paths, ready for Vercel deployment.
</objective>

<execution_context>
@/home/theohuang/.claude/get-shit-done/workflows/execute-plan.md
@/home/theohuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-deployment-pipeline/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create front-end scaffold files</name>
  <files>index.html, style.css, app.js, bazi-engine.js, solar.js, cities.js, i18n.js</files>
  <action>
    Create the following files at the repo root:

    index.html — minimal HTML5 boilerplate. Must:
    - Use <!DOCTYPE html> with lang="en"
    - Link style.css via <link rel="stylesheet">
    - Include <script src="bazi-engine.js"></script>, <script src="solar.js"></script>, <script src="cities.js"></script>, <script src="i18n.js"></script>, <script src="app.js"></script> before </body>
    - Have a <title>Four Pillars of Destiny</title>
    - Have a placeholder <h1> like "Four Pillars of Destiny" and a <p> "Coming soon."
    - No framework, no CDN imports, no inline scripts

    style.css — minimal CSS reset only:
    *, *::before, *::after { box-sizing: border-box; }
    body { margin: 0; font-family: sans-serif; }

    app.js — single comment: // Main entry point and UI logic
    bazi-engine.js — single comment: // Bazi chart calculation engine
    solar.js — single comment: // True solar time calculations
    cities.js — single comment: // City database and geocoding helpers
    i18n.js — single comment: // EN / zh-TW translations
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['index.html','style.css','app.js','bazi-engine.js','solar.js','cities.js','i18n.js'].forEach(f=>{ if(!fs.existsSync('/mnt/c/Users/26089/four-pillars/'+f)) throw new Error('Missing: '+f); }); console.log('All front-end scaffold files present');"</automated>
  </verify>
  <done>All 7 front-end files exist at repo root. index.html references all JS and CSS files. No framework dependencies present.</done>
</task>

<task type="auto">
  <name>Task 2: Create serverless function and Vercel config</name>
  <files>api/reading.js, vercel.json</files>
  <action>
    Create api/reading.js — Vercel serverless function stub:
    - Export a default handler function: module.exports = (req, res) => { ... }
    - GET request: return res.status(200).json({ status: 'ok', message: 'reading endpoint ready' })
    - POST request: return res.status(200).json({ status: 'ok', message: 'reading endpoint ready' })
    - Any other method: return res.status(405).json({ error: 'Method not allowed' })
    - No imports needed — no Claude API call in Phase 1

    Create vercel.json at repo root:
    {
      "functions": {
        "api/reading.js": {
          "maxDuration": 30
        }
      }
    }

    No custom routes needed — Vercel auto-routes /api/* to functions and serves index.html at root.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const fn=fs.readFileSync('/mnt/c/Users/26089/four-pillars/api/reading.js','utf8'); if(!fn.includes('module.exports')) throw new Error('Missing module.exports'); const cfg=JSON.parse(fs.readFileSync('/mnt/c/Users/26089/four-pillars/vercel.json','utf8')); if(!cfg.functions['api/reading.js'] || cfg.functions['api/reading.js'].maxDuration!==30) throw new Error('vercel.json misconfigured'); console.log('Serverless function and vercel.json valid');"</automated>
  </verify>
  <done>api/reading.js exports a handler responding to GET and POST with the health check JSON. vercel.json sets 30-second timeout for the function.</done>
</task>

</tasks>

<verification>
Run the automated verify commands for both tasks. Confirm:
- 9 files total exist (7 front-end + api/reading.js + vercel.json)
- index.html has no framework script tags (no react, vue, angular, svelte)
- vercel.json is valid JSON with maxDuration: 30
</verification>

<success_criteria>
All scaffold files exist at locked paths. index.html is pure HTML5 with no framework. api/reading.js returns health check JSON for GET and POST. vercel.json configures 30s timeout. Codebase is ready for `vercel deploy`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-deployment-pipeline/01-01-SUMMARY.md`
</output>
